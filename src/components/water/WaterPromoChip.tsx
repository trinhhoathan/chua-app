'use client';

/**
 * @deprecated Dùng TemplePromoChips trên layout chùa.
 * Giữ file để tránh import gãy — không còn tự hiện sớm.
 */
export function WaterPromoChip(_props: {
  primaryColor: string;
  templeName: string;
}) {
  return null;
}
